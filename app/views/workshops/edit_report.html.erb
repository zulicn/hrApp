<div class="projects-form">
	<%= form_for @report, url: { action: "update_report" }, html: { name: 'form-center' } do |f| %>

	  	<%= render partial: 'report_form', :locals => { :f => f, :submit_label => "Spasi" } %>
	<% end %>
</div>