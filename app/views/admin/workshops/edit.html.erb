<div class="projects-form">
	<%= form_for @workshop, url: { action: "update" }, html: { name: 'form-center' } do |f| %>
	  <%= render partial: 'form', :locals => { :f => f, :submit_label => "Promijeni" } %>
	<% end %>
</div>